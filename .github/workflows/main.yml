name: ci

on:
  push:
    branches:
      - 'main'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login
        uses: docker/login-action@v1
        with:
          registry: quay.io
          username:  crozzy+ci
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: quay.io/crozzy/local-clair:${{ github.sha }}

  update-vulnerability-db:
    needs: ['docker']
    runs-on: ubuntu-latest
    container: quay.io/crozzy/local-clair:${{ github.sha }}
    steps:
      - name: Update Vulnerabilities
        run: |
          DB_PATH=/matcher /bin/local-clair update
      - name: Bundle
        run: |
          ls -l /
